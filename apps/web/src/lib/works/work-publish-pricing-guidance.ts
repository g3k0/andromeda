export function getWorkPublishInitialPriceGuidance(): string {
  return "Optional. Leave blank to register the work on-chain without opening sales. You can list copies for sale or run auctions later.";
}

export function getWorkPublishNumberedCopiesGuidance(): string {
  return "Each minted copy is a numbered NFT. Collectors may value copies differently—for example, copy #1 versus copy #145—even when you set one initial list price above.";
}
