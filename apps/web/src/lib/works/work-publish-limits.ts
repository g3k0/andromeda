/** Minimum numbered copies an author may register for a work. */
export const WORK_PUBLISH_MIN_MAX_COPIES = 1;

/** Maximum numbered copies an author may register for a work (raise as the platform scales). */
export const WORK_PUBLISH_MAX_MAX_COPIES = 500;
