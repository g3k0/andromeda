# Copy to `.env.development.local` (gitignored) for local development secrets:
#   cp apps/web/.env.development.local.example apps/web/.env.development.local
#
# Alternatively, put the same variables in `.env.local`.

MONGODB_URI=mongodb://app_andromeda:<password>@127.0.0.1:27017/andromeda?authSource=andromeda
