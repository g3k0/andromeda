{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "if [ -f ../../pnpm-workspace.yaml ]; then cd ../..; fi; corepack enable && pnpm install --frozen-lockfile",
  "buildCommand": "next build"
}
